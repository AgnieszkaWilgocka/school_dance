{% extends 'base.html.twig' %}

{% block title %}
{{ 'title.myClasses_delete'|trans }}
{% endblock %}

{% block body %}
    <div class="my-classes_header">
        <div class="head-image"></div>
        <h1 class="h-header"><a href="{{ url('main_page') }}">Szkoła Tańca <span>Neon</span></a></h1>
    </div>
    <div class="form-container-admin">
<h1>{{ 'title.my_classes_delete'|trans }}</h1>

{{ form_start(form) }}
{{ form_widget(form) }}

<div class="form-group row float-sm-right">
    <input type="submit" value="{{ 'action.sign_off'|trans }}" class="btn btn-primary">
</div>
<div class="form-group row float-sm-left">
    <a href="{{ url('my_classes_index') }}" class="btn btn-link">
        {{ 'action.back_to_list'|trans }}
    </a>
</div>

{{ form_end(form) }}
    </div>
{% endblock %}